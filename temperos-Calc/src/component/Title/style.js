import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titlebox: {
        backgroundColor: '#333',
        height: 110,
    },
    text: {
        color: '#fff'
    }
});
export default styles